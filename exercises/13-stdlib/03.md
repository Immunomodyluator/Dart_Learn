# Задание 03 (13.2): dart:convert — 🖊 Написать код

**Тема:** 13. Стандартная библиотека Dart | **Подтема:** 13.2 dart:convert

---

Реализуй сериализацию и десериализацию JSON.

**Задача:**

1. Объяви класс `User` с полями `id: int`, `name: String`, `email: String`, `createdAt: DateTime`.

2. Добавь методы:
   - `User.fromJson(Map<String, dynamic> json)` — фабричный конструктор
   - `Map<String, dynamic> toJson()` — сериализация; `DateTime` кодируй как ISO 8601 строку

3. Напиши функцию `List<User> parseUsers(String jsonString)`, которая декодирует массив JSON и возвращает список `User`.

4. Напиши функцию `String encodeUsers(List<User> users)` с отступами (2 пробела через `JsonEncoder.withIndent`).

5. Обработай возможный `FormatException` при парсинге — верни пустой список.

```dart
import 'dart:convert';

// TODO: класс User и функции parseUsers, encodeUsers
```

<details>
<summary>Решение</summary>

```dart
import 'dart:convert';

class User {
  final int id;
  final String name;
  final String email;
  final DateTime createdAt;

  const User({
    required this.id,
    required this.name,
    required this.email,
    required this.createdAt,
  });

  factory User.fromJson(Map<String, dynamic> json) {
    return User(
      id: json['id'] as int,
      name: json['name'] as String,
      email: json['email'] as String,
      createdAt: DateTime.parse(json['createdAt'] as String),
    );
  }

  Map<String, dynamic> toJson() => {
        'id': id,
        'name': name,
        'email': email,
        'createdAt': createdAt.toIso8601String(),
      };

  @override
  String toString() => 'User($id, $name)';
}

List<User> parseUsers(String jsonString) {
  try {
    final data = jsonDecode(jsonString) as List<dynamic>;
    return data.map((e) => User.fromJson(e as Map<String, dynamic>)).toList();
  } on FormatException catch (e) {
    print('JSON parse error: $e');
    return [];
  }
}

String encodeUsers(List<User> users) {
  const encoder = JsonEncoder.withIndent('  ');
  return encoder.convert(users.map((u) => u.toJson()).toList());
}

void main() {
  const json = '''
  [
    {"id": 1, "name": "Alice", "email": "alice@example.com", "createdAt": "2024-01-15T10:30:00.000Z"},
    {"id": 2, "name": "Bob",   "email": "bob@example.com",   "createdAt": "2024-03-20T08:00:00.000Z"}
  ]
  ''';

  final users = parseUsers(json);
  print(users);          // [User(1, Alice), User(2, Bob)]

  final encoded = encodeUsers(users);
  print(encoded);        // красиво форматированный JSON с отступами

  print(parseUsers('not json')); // [], ошибка залогирована
}
```

`jsonDecode` возвращает `dynamic` — всегда приводи к конкретному типу (иначе ошибки во время выполнения). `DateTime.parse` принимает ISO 8601 строки. `JsonEncoder.withIndent` — удобная альтернатива `jsonEncode` если нужен форматированный вывод.

</details>
